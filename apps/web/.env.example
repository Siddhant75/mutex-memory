# Use the deployed Lambda/API Gateway base URL for live mode.
VITE_API_URL=https://your-api-id.execute-api.ap-south-1.amazonaws.com

# Set to exactly "true" only for the deterministic judge/demo fixture.
VITE_USE_FIXTURES=false
